export const environment = {
  production: false,
  url: 'http://localhost:3000/',
  config: {
    domain: '',
    clientId: '',
    audience: '',
    authorizationParams: {
      redirect_uri: window.location.origin
    }
  }
}