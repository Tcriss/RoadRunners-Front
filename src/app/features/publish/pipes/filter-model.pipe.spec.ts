import { FilterModelPipe } from './filter-model.pipe';

describe('FilterModelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModelPipe();
    expect(pipe).toBeTruthy();
  });
});
