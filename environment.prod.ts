
    export const environment = {
        production: true,
        url: 'undefined',
        config: {
            domain: 'undefined',
            clientId: 'undefined',
            audience: 'undefined',
            authorizationParams: {
                redirect_uri: window.location.origin
            }
        }
    };